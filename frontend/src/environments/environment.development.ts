// Development environment — swapped in for environment.ts by angular.json's
// "development" build configuration (the default for `ng serve`/`npm start`).
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8000/api',
  serverBaseUrl: 'http://localhost:8000',
};
